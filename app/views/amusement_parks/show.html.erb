<h1>Amusement Park: <%= @amusement_park.name %></h1>
<h3>Admission: <%= number_to_currency(@amusement_park.admission_cost) %></h3>

<div id="park-mechanics">
  <h3>Park Mechanics:</h3>
  <ul>
    <% @amusement_park.unique_mechanics.each do |mechanic| %>
      <li> <%= mechanic.name %> </li>
    <% end %>
  </ul>
</div>

<div id="park-rides">
  <h3>Park Rides:</h3>
  <ul>
    <% @amusement_park.rides.each do |ride| %>
    <li> <%= ride.name %> </li>
    <% end %>
  </ul>
</div>